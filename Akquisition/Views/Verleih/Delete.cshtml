@model efAkquisition.tbl_Verleih

@{
    ViewBag.Title = "Löschen";
}

<!DOCTYPE html>

<html>

<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>Wollen Sie diesen Verleih wirklich löschen?</h3>
    <hr />
    <div>
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Verleih)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Verleih)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <dl class="dl-horizontal">
                <dt />

                <dd>
                    <input type="submit" value="Löschen" class="btn btn-primary" />
                </dd>
            </dl>

            <div class="form-group">
                @Html.ActionLink("Zum Index", "Index")
            </div>
        }

        
    </div>
</body>
</html>
